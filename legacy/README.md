# Legacy Bridge Poller

This directory preserves the old `lark-cc-bridge/user_reaction_poller.py` sidecar code after `lark-cc-bridge` was narrowed to CC bot replies only.

The active reaction-only implementation is `../scripts/lark-reaction-as-user`. Keep this legacy file for reference when comparing behavior from the bridge-integrated version; do not launch it from `lark-cc-bridge`.
